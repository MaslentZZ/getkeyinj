<!DOCTYPE html><html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirect Key</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1e1e2f, #2e2e4d);
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
    }
    .container {
      text-align: center;
      padding: 40px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(10px);
      max-width: 90%;
      width: 400px;
    }
    h1 {
      font-size: 2.5em;
      margin-bottom: 10px;
      font-weight: 700;
      background: linear-gradient(to right, #00c6ff, #0072ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    p {
      margin-bottom: 30px;
      font-size: 1em;
      color: #dcdcdc;
    }
    button {
      background-image: linear-gradient(45deg, #6a11cb, #2575fc);
      color: white;
      border: none;
      padding: 15px 40px;
      font-size: 1em;
      border-radius: 40px;
      cursor: pointer;
      box-shadow: 0 8px 20px rgba(106, 17, 203, 0.4);
      transition: transform 0.2s, box-shadow 0.2s;
    }
    button:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 25px rgba(106, 17, 203, 0.6);
    }
    .footer {
      position: absolute;
      bottom: 15px;
      font-size: 0.9em;
      opacity: 0.6;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1> Key Generator</h1>
    <p>Please click the button below to get the latest key via our official link..</p>
    <button onclick="redirectToKey()">GET KEY</button>
  </div>  <div class="footer">&copy; Copyright Valrizk. All rights reserved.</div>  <script>
    function redirectToKey() {
      // Ganti link safelink ini setiap hari
      window.location.href = "https://google.com";
    }
  </script></body>
</html>
